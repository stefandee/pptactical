//---------------------------------------------------------------------------
#ifndef PPU_ResourcesH
#define PPU_ResourcesH
//---------------------------------------------------------------------------

typedef struct _TResources
{
  float mMetal;         // scrap metal  (tone)
  int   mEnergy;        // energy cells (pieces)
  int   mElectronic;    // electronic parts (pieces)
  int   mMech;          // mechanical parts (pieces)
} TTechResources;

#endif
